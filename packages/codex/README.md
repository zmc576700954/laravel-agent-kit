# laravel-agent-kit（Codex 包）

本目录用于作为“Codex 可直接引入”的独立仓库根目录（`AGENTS.md` 位于根目录）。

## 使用方式

- 项目级：将本仓库的 `AGENTS.md` 放在你的项目根目录。
- 全局级：若你的 Codex 环境支持导入/引用远程仓库作为全局规则入口，可直接引入本仓库（保持 `AGENTS.md` 位于仓库根目录）。

## 可选资产

`project-assets/` 提供脚本/模板类资产（可选）：

- `migrationctl-lite.ps1`
- `task-doc-template.md`
