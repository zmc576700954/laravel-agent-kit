# laravel-agent-kit（Codex 产物包）

本目录是 Codex 的可直接引入产物包，仓库根目录即包含 `AGENTS.md`。

## 使用方式

- 项目级：将本目录下的 `AGENTS.md` 放到你的项目根目录。
- 全局级：若你的 Codex 支持导入远程仓库作为全局规则入口，可直接引入本仓库（保证 `AGENTS.md` 位于仓库根目录）。

## 可选（结构化规则）

若你的客户端支持额外规则目录加载，可使用 `rules/` 作为可选的结构化规则源；不配置也不影响默认使用。
