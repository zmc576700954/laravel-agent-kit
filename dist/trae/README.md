# laravel-agent-kit（Trae 产物包）

本目录是 Trae 的 skills 产物包。

## 使用方式

- 将 `skills/` 下的各 skill 目录复制到 Trae 的 skills 安装目录即可使用。
- 推荐安装：`laravel-agent-router` + `laravel-agent-global`，再按需安装场景 skills 与 profile skills。
