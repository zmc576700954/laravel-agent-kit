# laravel-agent-kit（Claude Code 产物包）

本目录是 Claude Code 的可直接引入产物包，仓库根目录即包含 `CLAUDE.md`。

## 使用方式

- 项目级：将本目录下的 `CLAUDE.md` 放到你的项目根目录。

## 可选（结构化规则）

若你的客户端支持额外规则目录加载，可使用 `rules/` 作为可选的结构化规则源；不配置也不影响默认使用。
